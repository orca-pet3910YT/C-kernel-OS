#ifndef MATH_H
#define MATH_H
int sqrt(int n); // "shopping cart"
char is_prime(int n); // checks if n is a prime number
#define min(x, y) x < y ? x : y
#define max(x, y) x > y ? x : y
#define PI 3.14159265358979323846 // apple pie
#define abs(n) n < 0 ? -n : n // |n|
int x_pow_2y(int x, int y); // x^(2y)
#endif
